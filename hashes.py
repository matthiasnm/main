#!/bin/bash

#Comparing the hashes of two files.

a = ()

b = () 

a = input('Enter the first Hash of file1: ')

b = input('Enter the second Hash of file2: ')

if a == b:
    print ('The hashes are the same')

else: 
    print('The hashes are not the same check that the hashes were entered correctly. ')



